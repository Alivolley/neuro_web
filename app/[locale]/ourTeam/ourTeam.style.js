import styled from '@emotion/styled';

const OurTeamCard = styled.div(() => ({
   display: 'flex',

   '& .leftSide': {
      //
   },

   '& .rightSide': {
      width: '100%',
      height: '100%',
   },
}));

export default OurTeamCard;
